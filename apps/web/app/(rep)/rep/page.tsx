export default function RepPortalPlaceholderPage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center p-24">
      <h1 className="text-2xl font-semibold">Rep Portal</h1>
      <p className="text-muted-foreground mt-2">Placeholder shell for the (rep) route group.</p>
    </main>
  );
}
